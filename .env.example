VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: override password-reset redirect (must match Supabase Auth → Redirect URLs).
# Default is the app deep link: umbrella-deadlock-db://auth/callback (opens the installed Electron app).
# VITE_SUPABASE_REDIRECT_URL=https://your-app.example.com
